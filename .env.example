# ===============================================
# Azure Cloud Native RAG - Environment Variables
# ===============================================
# 
# SECURITY INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your actual values in .env
# 3. NEVER commit .env to Git (it's in .gitignore)
# 4. Keep your API keys and passwords secret
# ===============================================

# Django Configuration
DJANGO_SECRET_KEY=generate_a_random_secret_key_here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Google Gemini API
# Get your API key from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your_google_gemini_api_key_here

# PostgreSQL Database
POSTGRES_DB=library_db
POSTGRES_USER=admin
POSTGRES_PASSWORD=your_secure_database_password_here
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# MongoDB (if using)
MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_USER=admin
MONGO_PASSWORD=your_secure_mongo_password_here

# CORS Settings (comma-separated origins for production)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Vault Settings (if using HashiCorp Vault)
VAULT_ADDR=http://localhost:8200
VAULT_TOKEN=root

# ===============================================
# PRODUCTION DEPLOYMENT NOTES:
# ===============================================
# For production on Azure:
# - Use Azure Key Vault for secrets management
# - Set DEBUG=False
# - Generate strong SECRET_KEY (50+ characters)
# - Use Azure Database for PostgreSQL
# - Configure proper ALLOWED_HOSTS
# - Enable SSL/HTTPS
# ===============================================
